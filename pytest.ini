[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: requires real models and GPU
    unit: pure logic tests, no models needed
    e2e: end-to-end tests requiring live voscript server at localhost:8780
